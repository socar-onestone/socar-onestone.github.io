name: Check korean grammar on pull request comment

on:
  issue_comment:
    types: [created]

jobs:
  grammar_check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Deploy
        run: |
          echo "Deploying..."
